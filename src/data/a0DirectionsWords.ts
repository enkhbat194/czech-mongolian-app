import { czechWords, type CzechWord } from './czechWords';

/** Canonical source: czechWords.ts */
export const a0DirectionsWords: CzechWord[] = czechWords.filter((card) => card.lessonId === 'l004');
